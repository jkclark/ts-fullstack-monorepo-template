{
    "name": "ts-fullstack-monorepo-template",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm --filter frontend dev",
        "clean": "rm -rf node_modules **/node_modules"
    }
}
